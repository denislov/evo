diff --git a/crates/desktop/src/conversation.rs b/crates/desktop/src/conversation.rs
index 138bd7d..7e33846 100644
--- a/crates/desktop/src/conversation.rs
+++ b/crates/desktop/src/conversation.rs
@@ -1001,0 +1002 @@ mod tests {
+        let allocations_before = crate::allocation_probe::snapshot();
@@ -1004,0 +1006,3 @@ mod tests {
+        let allocations_after = crate::allocation_probe::snapshot();
+        let hydration_allocations = allocations_after.0.saturating_sub(allocations_before.0);
+        let hydration_allocated_bytes = allocations_after.1.saturating_sub(allocations_before.1);
@@ -1039,0 +1044,2 @@ mod tests {
+             hydration_allocations={hydration_allocations}\t\
+             hydration_allocated_bytes={hydration_allocated_bytes}\t\
diff --git a/crates/desktop/src/lib.rs b/crates/desktop/src/lib.rs
index 436ffb8..3990b27 100644
--- a/crates/desktop/src/lib.rs
+++ b/crates/desktop/src/lib.rs
@@ -19,0 +20,53 @@ mod shell;
+#[cfg(test)]
+mod allocation_probe {
+    use std::alloc::{GlobalAlloc, Layout, System};
+    use std::sync::atomic::{AtomicU64, Ordering};
+
+    struct CountingAllocator;
+    static ALLOCATION_COUNT: AtomicU64 = AtomicU64::new(0);
+    static ALLOCATED_BYTES: AtomicU64 = AtomicU64::new(0);
+
+    #[global_allocator]
+    static ALLOCATOR: CountingAllocator = CountingAllocator;
+
+    unsafe impl GlobalAlloc for CountingAllocator {
+        unsafe fn alloc(&self, layout: Layout) -> *mut u8 {
+            let pointer = unsafe { System.alloc(layout) };
+            if !pointer.is_null() {
+                ALLOCATION_COUNT.fetch_add(1, Ordering::Relaxed);
+                ALLOCATED_BYTES.fetch_add(layout.size() as u64, Ordering::Relaxed);
+            }
+            pointer
+        }
+
+        unsafe fn alloc_zeroed(&self, layout: Layout) -> *mut u8 {
+            let pointer = unsafe { System.alloc_zeroed(layout) };
+            if !pointer.is_null() {
+                ALLOCATION_COUNT.fetch_add(1, Ordering::Relaxed);
+                ALLOCATED_BYTES.fetch_add(layout.size() as u64, Ordering::Relaxed);
+            }
+            pointer
+        }
+
+        unsafe fn dealloc(&self, pointer: *mut u8, layout: Layout) {
+            unsafe { System.dealloc(pointer, layout) };
+        }
+
+        unsafe fn realloc(&self, pointer: *mut u8, layout: Layout, new_size: usize) -> *mut u8 {
+            let pointer = unsafe { System.realloc(pointer, layout, new_size) };
+            if !pointer.is_null() {
+                ALLOCATION_COUNT.fetch_add(1, Ordering::Relaxed);
+                ALLOCATED_BYTES.fetch_add(new_size as u64, Ordering::Relaxed);
+            }
+            pointer
+        }
+    }
+
+    pub(crate) fn snapshot() -> (u64, u64) {
+        (
+            ALLOCATION_COUNT.load(Ordering::Relaxed),
+            ALLOCATED_BYTES.load(Ordering::Relaxed),
+        )
+    }
+}
+
